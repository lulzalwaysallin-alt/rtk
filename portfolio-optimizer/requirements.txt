# 포트폴리오 최적화 분석기 실행에 필요한 패키지
# 설치:  pip install -r requirements.txt

streamlit>=1.49          # 웹 화면
pandas>=2.1              # 표 계산
numpy>=1.26              # 수치 연산
scipy>=1.11              # 최적화 (SLSQP, L-BFGS-B)
plotly>=5.18             # 차트
finance-datareader>=0.9.9  # 국내·해외 시세
yfinance>=0.2.40         # 시세 보조 경로 (FinanceDataReader 실패 시)
XlsxWriter>=3.1          # 엑셀 내보내기
openpyxl>=3.1            # 엑셀 읽기 (테스트용)
